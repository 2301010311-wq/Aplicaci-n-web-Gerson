NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=polleria_gerson

# Needed by Prisma CLI (migrate, generate, db push)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/polleria_gerson?schema=public

JWT_SECRET=change-this-secret-key
